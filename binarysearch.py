class list:
	mydict = {}
	a = list.length()
	b = string.step()
	x = index
	length = number(len(array))


class binarysearch(list):
	def __init__ (self,a='length of list',b='step'):
		self.a = length
		self.b = count(word)
			

	def search(self,n):
		count = result.count(word)
		worddict[word] = word
		x = 0
		if x = x+1:
			return count

	

					

		 	
